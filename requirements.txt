# WatsonX Clone — dependencies.
#
# This is a self-contained demo clone of the Seller Dashboard: every external
# connection (IBM ISC, the IBM install-base portals, ZoomInfo, Salesloft, Google
# News) is MOCKED with deterministic fake data (see fake_data.py / mock_salesloft.py),
# so there is NO browser automation and NO provider SDKs — just Flask + openpyxl.
#
# Install:  python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#           .venv/bin/python3 run_pipeline.py

openpyxl==3.1.5      # Excel file-handoff contract between every pipeline step
flask==3.1.3         # the dashboard web app (run_pipeline.py)
